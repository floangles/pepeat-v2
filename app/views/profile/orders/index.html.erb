<% if @orders.count == 0 %>
  <h3>Vous n'avez fait aucune réservation</h3>
<% else %>
  <% @orders.each do |order| %>
    <div class="card-globale">
      <h3 class= "text-center title" ><%= order.meal.title %></h3>
      <div class="container">
        <div class="row">
          <div class="col-xs-6">
            <%= render "ordercard", meal: order.meal %>
          </div>
          <div class="col-xs-6">
            <%= render "infoorder", order: order %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
